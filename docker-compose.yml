postgres:
  image: postgres
  ports:
    - "5432"

rails:
  build: .
  ports:
    - "3000"
  links:
   - postgres
   - redis

redis:
  image: redis

nginx:
  build: ./nginx
  links:
    - rails
  ports:
    - "80:80"